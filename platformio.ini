[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
lib_deps =
  emelianov/modbus-esp8266@^4.1.0

; UART2 RX on GPIO16, Tag-in-range on GPIO17
; The code uses HardwareSerial(2) at 9600 baud.
